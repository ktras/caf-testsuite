COMPILER="cray"
FC="ftn"
CC="cc"

FFLAGS="-eZ -O3 -Omsgs,nointerchange"

LAUNCHER="srun -N 2 -n 2 --cpu-bind=cores -v -l -- /usr/bin/time "
EXEC_OPTIONS=""

COMPILE_CMD="${FC} ${FFLAGS} ${FFLAGS_BENCH_DEFS}"

